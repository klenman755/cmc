V1 - Long normal version

program             block*

block               declaration
                    statement

declaration         VARIABLE_TYPE IDENTIFIER OPERATOR [INTEGER_LITERAL | BOO_VALUE | IDENTIFIER];                   NUMBER x = 1;
                    VARIABLE_TYPE IDENTIFIER ;                                                						NUMBER x;
                    METHOD IDENTIFIER START_BRACKET declarationList END_BRACKET statement* ENDMETHOD
                    ARRAY VARIABLE_TYPE IDENTIFIER [ valueList | parameterList ] ENDARRAY


statement           operation
                    evaluationBlock
		            methodCall

operation           IDENTIFIER OPERATOR IDENTIFIER ;                                  x=y;
                    IDENTIFIER OPERATOR INTEGER_LITERAL ;                             x=1;
 		            IDENTIFIER OPERATOR BooValue ;                                    x=true;

                    IDENTIFIER OPERATOR [ IDENTIFIER | INTEGER_LITERAL ] [ OPERATOR IDENTIFIER | INTEGER_LITERAL ]* ;  x=x+1 x=1+1+1 x=x+x+x x=x+1+x

methodCall          IDENTIFIER START_BRACKET parameterList END_BRACKET

evaluationBlock     IF expression THEN statement* ENDIF
                    WHILE expression THEN statement* ENDWHILE

parameterList       IDENTIFIER [, IDENTIFIER]*
                    e

declarationList     VARIABLE_TYPE IDENTIFIER
                    VARIABLE_TYPE IDENTIFIER, VARIABLE_TYPE IDENTIFIER...
                    e

valueList           INTEGER_LITERAL [ ,INTEGER_LITERAL ] *
                    BOO_VALUE [ ,BOO_VALUE ] *

expression          IDENTIFIER OPERATOR IDENTIFIER
                    IDENTIFIER OPERATOR INTEGER_LITERAL
		            IDENTIFIER OPERATOR BOO_VALUE
		            BOO_VALUE OPERATOR IDENTIFIER
				    INTEGER_LITERAL OPERATOR IDENTIFIER
				    